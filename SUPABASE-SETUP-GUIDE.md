# 🔧 Руководство по настройке Supabase для КРАН-МОНТАЖ

## 📋 Содержание

1. [Создание проекта](#создание-проекта)
2. [Настройка базы данных](#настройка-базы-данных)
3. [Настройка админ-панели](#настройка-админ-панели)
4. [Настройка Storage](#настройка-storage)
5. [Создание администратора](#создание-администратора)
6. [Настройка переменных окружения](#настройка-переменных-окружения)
7. [Проверка работы](#проверка-работы)

---

## 1. Создание проекта

### Шаг 1: Создайте аккаунт и проект
1. Перейдите на [supabase.com](https://supabase.com)
2. Нажмите **Start your project**
3. Войдите через GitHub или создайте аккаунт
4. Нажмите **New Project**
5. Заполните данные:
   - **Name**: `kran-montaj` (или другое название)
   - **Database Password**: Сохраните пароль в надежном месте!
   - **Region**: Выберите ближайший регион (например, `eu-central-1`)
6. Нажмите **Create new project**

### Шаг 2: Дождитесь создания проекта
- Обычно это занимает 1-2 минуты
- После создания вы увидите Dashboard проекта

---

## 2. Настройка базы данных

### Шаг 1: Откройте SQL Editor
1. В левом меню выберите **SQL Editor**
2. Нажмите **+ New query**

### Шаг 2: Выполните основной SQL скрипт
1. Откройте файл `supabase-complete-setup.sql` из проекта
2. Скопируйте весь его содержимое
3. Вставьте в SQL Editor
4. Нажмите **Run** (или Ctrl+Enter)

### Шаг 3: Проверьте результат
После выполнения скрипта должны быть созданы:

**Таблицы:**
- `categories` - Категории продуктов и услуг
- `services` - Услуги компании
- `products` - Каталог продукции
- `contacts` - Контактная информация
- `general_content` - Общий контент сайта
- `page_content` - Контент страниц
- `about_content` - Контент страницы "О компании"
- `company_stats` - Статистика компании

**Представления (Views):**
- `products_with_category` - Продукты с категориями
- `services_with_category` - Услуги с категориями

### Шаг 4: Проверьте таблицы
1. В левом меню выберите **Table Editor**
2. Убедитесь, что все таблицы созданы
3. Проверьте, что начальные данные добавлены

---

## 3. Настройка админ-панели

### Шаг 1: Выполните SQL скрипт для админ-панели
После выполнения основного скрипта, выполните дополнительный скрипт:

1. Откройте **SQL Editor**
2. Создайте новый запрос
3. Откройте файл `supabase-admin-setup.sql`
4. Скопируйте и вставьте содержимое
5. Нажмите **Run**

### Что создается:

**Таблицы:**
- `admin_roles` - Роли администраторов (super_admin, admin, editor, viewer)
- `admin_profiles` - Профили админов (связаны с auth.users)
- `admin_activity_logs` - Логи действий администраторов
- `admin_sessions` - Сессии администраторов
- `system_settings` - Системные настройки сайта

**Представления:**
- `admin_profiles_with_roles` - Профили с информацией о ролях
- `recent_admin_activity` - Последние действия админов

**Функции:**
- `handle_new_admin_user()` - Автоматически создает профиль при регистрации
- `update_admin_last_login()` - Обновляет время последнего входа
- `log_admin_action()` - Логирует действия админа
- `check_admin_permission()` - Проверяет разрешения

**Роли по умолчанию:**
| Роль | Описание |
|------|----------|
| `super_admin` | Полный доступ ко всему |
| `admin` | Управление контентом |
| `editor` | Редактирование контента |
| `viewer` | Только просмотр |

---

## 4. Настройка Storage

### Шаг 1: Создайте Bucket
1. В левом меню выберите **Storage**
2. Нажмите **New bucket**
3. Настройки:
   - **Name**: `img`
   - **Public bucket**: ✅ Включить
   - **File size limit**: 10 MB (или больше)
   - **Allowed MIME types**: `image/*` (опционально)
4. Нажмите **Create bucket**

### Шаг 2: Создайте структуру папок
1. Откройте созданный bucket `img`
2. Создайте следующие папки:
   - `products` - для изображений продуктов
   - `services` - для изображений услуг
   - `page-content` - для контента страниц
   - `general` - для общих изображений
   - `about` - для страницы "О компании"

### Шаг 3: Настройте политики (опционально)
Если вам нужны дополнительные политики безопасности:
1. Откройте **SQL Editor**
2. Выполните скрипт из файла `supabase-storage-setup.sql`

---

## 5. Создание администратора

### Шаг 1: Перейдите в Authentication
1. В левом меню выберите **Authentication**
2. Нажмите на вкладку **Users**

### Шаг 2: Создайте пользователя
1. Нажмите **Add user** → **Create new user**
2. Заполните данные:
   - **Email**: `admin@kran-montaj.uz` (ваш email)
   - **Password**: Создайте надежный пароль
   - **Auto Confirm User**: ✅ Включить
3. Нажмите **Create user**

### Шаг 3: Проверьте пользователя
- Пользователь должен появиться в списке
- Статус `Email confirmed` должен быть ✅

### Шаг 4: Назначьте роль Super Admin
После создания пользователя, выполните SQL запрос:

1. Откройте **SQL Editor**
2. Выполните запрос (замените email на ваш):

```sql
-- Назначить роль super_admin
UPDATE public.admin_profiles
SET role_id = (SELECT id FROM public.admin_roles WHERE name = 'super_admin')
WHERE email = 'admin@kran-montaj.uz';
```

### Шаг 5: Проверьте профиль админа
Выполните запрос для проверки:

```sql
SELECT * FROM public.admin_profiles_with_roles;
```

Вы должны увидеть своего администратора с ролью `super_admin`.

---

## 6. Настройка переменных окружения

### Шаг 1: Получите ключи API
1. В левом меню выберите **Settings** → **API**
2. Скопируйте:
   - **Project URL**: `https://xxxxx.supabase.co`
   - **anon public**: Публичный ключ для клиента

### Шаг 2: Обновите конфигурацию проекта
Откройте файл `app/lib/supabase.ts` и обновите значения:

```typescript
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = 'https://ВАШ_PROJECT_REF.supabase.co';
const supabaseAnonKey = 'ВАШ_ANON_KEY';

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

### Шаг 3: Создайте файл .env.local (рекомендуется)
Для большей безопасности создайте файл `.env.local`:

```env
NEXT_PUBLIC_SUPABASE_URL=https://ВАШ_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ВАШ_ANON_KEY
```

И обновите `app/lib/supabase.ts`:

```typescript
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

---

## 7. Проверка работы

### Шаг 1: Запустите проект
```bash
npm run dev
```

### Шаг 2: Проверьте страницы
1. Откройте http://localhost:3000 - Главная страница
2. Откройте http://localhost:3000/catalog - Каталог продукции
3. Откройте http://localhost:3000/services - Услуги
4. Откройте http://localhost:3000/contacts - Контакты
5. Откройте http://localhost:3000/about - О компании

### Шаг 3: Проверьте админ-панель
1. Откройте http://localhost:3000/admin/login
2. Войдите с созданными данными администратора
3. Проверьте работу разделов:
   - Dashboard
   - Каталог
   - Услуги
   - Категории
   - Контакты
   - Общий контент
   - Контент страниц

---

## 📊 Структура базы данных

```
┌─────────────────────────────────────────────────────────────────────┐
│                          БАЗА ДАННЫХ                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────┐      ┌─────────────┐      ┌─────────────┐         │
│  │  categories │◄────►│  products   │      │  services   │         │
│  │             │      │             │      │             │         │
│  │ id          │      │ id          │      │ id          │         │
│  │ name        │      │ title       │      │ name        │         │
│  │ slug        │      │ description │      │ description │         │
│  │ description │      │ image_url   │      │ image_url   │         │
│  └─────────────┘      │ category_id │──────│ category_id │         │
│                       │ characteristics │  │ order       │         │
│                       │ price       │      └─────────────┘         │
│                       └─────────────┘                               │
│                                                                     │
│  ┌─────────────┐      ┌─────────────────┐  ┌─────────────┐         │
│  │  contacts   │      │ general_content │  │page_content │         │
│  │             │      │                 │  │             │         │
│  │ type        │      │ key             │  │ page        │         │
│  │ value       │      │ section         │  │ section     │         │
│  │ label       │      │ title           │  │ title       │         │
│  │ order       │      │ text            │  │ content     │         │
│  └─────────────┘      │ image_url       │  │ image_url   │         │
│                       └─────────────────┘  └─────────────┘         │
│                                                                     │
│  ┌───────────────┐    ┌───────────────┐                            │
│  │ about_content │    │ company_stats │                            │
│  │               │    │               │                            │
│  │ block_key     │    │ stat_key      │                            │
│  │ title         │    │ label         │                            │
│  │ content       │    │ value         │                            │
│  │ icon          │    │ icon          │                            │
│  │ order         │    │ order         │                            │
│  └───────────────┘    └───────────────┘                            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔒 Безопасность

### Row Level Security (RLS)
Все таблицы защищены политиками RLS:
- **Чтение**: Доступно всем пользователям
- **Запись/Обновление/Удаление**: Только авторизованным пользователям

### Storage Policies
- Публичное чтение изображений
- Загрузка/удаление только для авторизованных

---

## ❓ Устранение неполадок

### Ошибка "relation does not exist"
- Убедитесь, что SQL скрипт выполнен полностью
- Проверьте, что нет ошибок в SQL Editor

### Ошибка авторизации
- Проверьте правильность ключей API
- Убедитесь, что пользователь подтвержден

### Изображения не загружаются
- Проверьте, что bucket `img` создан и публичный
- Проверьте права доступа к Storage

### Данные не отображаются
- Проверьте Console в браузере на наличие ошибок
- Убедитесь, что RLS политики настроены правильно

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль разработчика в браузере
2. Проверьте логи в Supabase Dashboard
3. Обратитесь к [документации Supabase](https://supabase.com/docs)

---

✅ **Готово!** Ваш проект КРАН-МОНТАЖ теперь полностью настроен и готов к работе!

